Crewai Tools require pip install 'google-adk[extensions]'

Run the command below to install it.
```
cd ~/agent/
python3 -m pip install google-adk[extensions] -r ./requirements.txt
```

Interaction

```
Get 10 of the latest headlines from AP News.
```